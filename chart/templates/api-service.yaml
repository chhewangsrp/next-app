apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.api.name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}
      name: {{ .Values.api.name }}
  selector:
    app: {{ .Values.api.name }}
